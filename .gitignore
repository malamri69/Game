node_modules/
dist/
*.log
.env
.env.local
.DS_Store
coverage/

# Design canvas: keep the .dc.html source + canvas.json, not the seeded
# multi-MB editor payload (regenerate with seed-canvas.mjs when needed).
design-mockup/*.html
!design-mockup/*.dc.html
**/tmp-play-cli.mjs
