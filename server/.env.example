PORT=3000

# Only needed once the Prisma schema (server/prisma/schema.prisma) is wired
# in — the server currently runs entirely in-memory and doesn't read this.
DATABASE_URL=postgresql://unknown_king:unknown_king@localhost:5432/unknown_king
